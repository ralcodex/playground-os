[DO NOW]
-- figure out typing and parsing in terminal (and maybe reuse some of that knowledge or code to create a calculator) // half done
-- figure out how to load apps and make a simple test app (maybe just a blank screen and some text) 

[CAN WAIT]
-- add the intro animation
-- make a settings app (probably just some commands in the terminal)
-- make a calculator app (empty folder waiting in "apps")
-- add a shutdown animation (maybe just the logo fading out?) 
-- add a crash screen 

[OPTIONAL]
-- maybe an mspaint clone?????????? (optional, but would be a fun project)
-- make a text editor app (maybe i could reuse code from the terminal for text input?)